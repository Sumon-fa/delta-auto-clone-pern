import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
  section: { margin: theme.spacing(8, 8, 8, 8) },
  title: {
    textAlign: 'center',
    marginBottom: '50px !important'
  }
}));
